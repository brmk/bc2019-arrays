// S1

export const returnEmpty = input => {
  return [];
};

// Write a function to check if the input is array
export const t1 = input => {
  // implementation
};

// clone array (not deeply)
export const t2 = input => {};

// stringify array
// input: ['a','b','c']
// output: "a, b, c."
export const t3 = input => {};

// what is the most frequent element of array and the number of times it appears
// input: [3, 'a', 'a', 'a', 2, 3, 'a', 3, 'a', 2, 4, 9, 3]
// ouput: "a - 5 times"
//
// input: []
// output: ""
//
// input: ['a']
// output: "a - 1 time"
//
// input: ['a','b']
// output: 'a'
export const t4 = input => {};

// make the array flat
// input: [[0,1,2], 3, 4, 5, [6,7]]
// output: [0,1,2,3,4,5,6,7]
export const t5 = input => {};

// check if all the array elements are less than threshold
// returns boolean. true if all elements are less than threshold
export const t6 = (input, threshold) => {};
